name: dividendendackel
description: "DividendenDackel - the dachshund that fetches your dividends. A local-first dividend and portfolio-event companion for Android and Linux."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.61.1+108

environment:
  sdk: 3.13.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  # Stable SHA-256 row fingerprints make repeated imports idempotent.
  crypto: 3.0.7
  # RFC 4180-compatible parsing for local, previewed portfolio imports.
  csv: 8.0.0
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: 1.0.9
  # Exact decimal arithmetic. Money must never be represented as a binary
  # floating-point number (Vision.md §79: never fabricate values).
  decimal: 3.2.6
  # Local SQLite persistence (Vision.md §35). drift_flutter supplies the
  # platform database directory and the bundled native sqlite3 for Android
  # and Linux.
  drift: 2.34.3
  drift_flutter: 0.3.1
  # Native read-only import selection and Linux export dialogs. Android export
  # uses ACTION_CREATE_DOCUMENT because this plugin does not save there.
  file_selector: 1.1.0
  flutter:
    sdk: flutter
  # Cross-platform OS notifications. Linux supports immediate delivery but
  # not background scheduling, so due events are reconciled on app activity.
  flutter_local_notifications: 22.3.0
  flutter_localizations:
    sdk: flutter
  # One state-management approach across the app (Vision.md §54).
  flutter_riverpod: 3.4.2
  # User-supplied provider credentials live in Android Keystore-backed or
  # Linux Secret Service-backed storage, never in plain preferences.
  flutter_secure_storage: 11.0.0
  go_router: 18.0.0
  # Provider HTTP transport. Pinned exactly for reproducible Android/Linux
  # artifacts and injected as a Client so adapters remain fixture-testable.
  http: 1.6.0
  # Reads the real version and build number from the installed package, so
  # the About screen can never disagree with the artifact (Vision.md §62).
  package_info_plus: 10.2.1
  # Small, non-sensitive app preferences shared by Android and Linux. Secrets
  # use platform-secure storage in F11 instead.
  shared_preferences: 2.5.5
  # Opens publisher pages in the platform browser; article bodies are never
  # copied into the app (Vision.md §18). Supports Android and Linux.
  url_launcher: 6.3.2

dev_dependencies:
  build_runner: 2.16.0
  drift_dev: 2.34.5
  flutter_launcher_icons: 0.14.4
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: 6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    # The repository's own changelog, shipped verbatim and parsed at runtime so
    # the app and the repo can never disagree about what changed.
    - CHANGELOG.md
    # Illustrative offline dataset, so the app is usable with no API key.
    - assets/sample/dataset.json
    # Versioned, user-overridable starter assumptions for dividend taxation.
    - assets/tax/withholding_rates.json
    # Window icon, loaded at runtime by the Linux runner.
    - assets/branding/icon.png

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Launcher icon generation (Vision.md §24). Regenerate with:
#   dart run flutter_launcher_icons
flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/branding/icon.png"
  # Adaptive icons let the launcher mask the shape, so the Dackel lives in a
  # separate foreground sized to the safe zone and cannot be clipped.
  adaptive_icon_background: "#12503F"
  adaptive_icon_foreground: "assets/branding/icon-foreground.png"
  min_sdk_android: 29
